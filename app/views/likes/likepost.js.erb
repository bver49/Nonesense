$('#likebtn-word').text('Unlike');
$('#likebtn').attr('href', "/<%= @post.id %>/unlike_post")
             .attr('id', 'unlikebtn');
var like = $('#likeamt').text();
$('#likeamt').addClass('red')
             .text(parseInt(like)+1);
$('a.ui.button').addClass('red');
