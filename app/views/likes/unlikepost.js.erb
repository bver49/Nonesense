$('#unlikebtn').text('Like')
             .attr('href', "/<%= @like.post_id %>/like_post")
             .attr('id', 'likebtn');
             var like = $('#likeamt').text();
             $('#likeamt').text(parseInt(like)-1)
