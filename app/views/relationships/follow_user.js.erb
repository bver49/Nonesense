$('#btnbar-<%= @user.id %> #follow-button').attr('class', 'tiny ui red basic button')
                  .text('追蹤中')
                  .attr('href', "/<%= @user.id %>/unfollow_user")
                  .attr('id', 'unfollow-button');
